# PopLock API Configuration
# Location: /etc/poplock-api.conf
# Permissions: chmod 600 /etc/poplock-api.conf

# API key for write endpoints — set this to a long random string
# Generate one with: openssl rand -hex 32
API_KEY=REPLACE_WITH_YOUR_SECRET_KEY

# Port to listen on
PORT=6767
